// Shared theme type — formerly lived in the sidebar's TweakRoot, which moved to
// the dialkit repo. Move UI components resolve 'system' against the media query.
export type TweakTheme = 'light' | 'dark' | 'system';
